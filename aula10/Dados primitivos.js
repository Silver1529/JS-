// Dados primitivos
// String, Number, Undefined, Null, Boolean, Symbol, BigInt
// String -> Texto 
// Number -> Números    
// Undefined -> indefinido, não aponta para local nenhum na memória
// Null -> nulo, não aponta para local nenhum na memória, ou seja não tem valor o usario não escolheu um valor
// Boolean -> lógico, true ou false
// Symbol -> simbolo, valor único e imutável
// BigInt -> números inteiros grandes
const nome1 = 'Miguel'; //string 
const nome2 = 'João';  //string
const nome3 = 'eduardo'; //string
const num1 = 10; //number 
const num2 = 15.87; //number 
let nomeAluno; // undefined -> não aponta para local nenhum na memória 
let sobrebrenomeAluno = null; //nulo -> não aponta para local nenhum na memória, ou seja não tem valor o usario não escolheu um valor 
const boolean = true; //boolean -> true ou false (lógico) 
const aprovado = false; // boolean é valor logico 
const